# 1 черезToken не работает
# 2 один раз прошёл авторизацию через сайт и потом через токен push получился, теперь даже окно выбора не выводится
# 3 echo "url=https://github.com" | git credential reject
# 4 git push origin test_james_saucedemo
